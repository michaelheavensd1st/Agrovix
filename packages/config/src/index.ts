/**
 * @agrovix/config
 * -----------------------------------------------------------------------
 * Shared configuration presets (ESLint, Tailwind, TypeScript). See the
 * `./eslint`, `./tailwind`, `./typescript` sub-paths for concrete files.
 */

export const AGROVIX_CONFIG_VERSION = '0.1.0';
